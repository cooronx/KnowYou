'use client';import {useState} from'react';import{useRouter}from'next/navigation';export default function Home(){const[n,setN]=useState('');const[c,setC]=useState('DEMO01');const r=useRouter();async function go(url:string){const x=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:n})}).then(x=>x.json());if(x.error)return alert(x.error);localStorage.setItem('playerId',x.playerId);r.push('/room/'+(x.code||c))}return <main><h1>KnowYou</h1><p>游客演示版 · 固定测试故事</p><input placeholder="昵称" value={n} onChange={e=>setN(e.target.value)}/><button onClick={()=>go('/api/rooms')}>创建房间</button><input value={c} onChange={e=>setC(e.target.value)}/><button onClick={()=>go('/api/rooms/'+c+'/join')}>加入房间</button></main>}
