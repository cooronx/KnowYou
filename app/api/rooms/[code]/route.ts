import {NextResponse} from 'next/server';import {getRoom} from '@/lib/room-store';export async function GET(_:Request,{params}:{params:Promise<{code:string}>}){const r=getRoom((await params).code);return r?NextResponse.json(r):NextResponse.json({error:'房间不存在'},{status:404})}
