import {NextResponse} from 'next/server';import {getRoom,turn} from '@/lib/room-store';export async function POST(req:Request,{params}:{params:{code:string}}){try{const r=getRoom(params.code);if(!r)throw Error('不存在');const {playerId,choiceId,text}=await req.json();turn(r,playerId,{choiceId,text:text||''});return NextResponse.json(r)}catch(e){return NextResponse.json({error:(e as Error).message},{status:400})}}
