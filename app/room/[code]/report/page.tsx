'use client';import{useEffect,useState}from'react';import{useParams}from'next/navigation';export default function Report(){const{code}=useParams<{code:string}>();const[r,setR]=useState<any>();useEffect(()=>{fetch('/api/rooms/'+code).then(x=>x.json()).then(setR)},[code]);return <main><h1>共同总结</h1>{r?.report&&<><ul>{r.report.common.map((x:string)=><li key={x}>{x}</li>)}</ul><p>{r.report.complement}</p><ul>{r.report.topics.map((x:string)=><li key={x}>{x}</li>)}</ul></>}</main>}
