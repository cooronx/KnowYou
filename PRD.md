# KnowYou · 产品需求文档（PRD）

> 一句话：两个陌生人被匹配进同一个由 AI 推演的知乎故事剧本，在几个回合的共同选择里，比聊一晚天更了解对方。

- 版本：v1.0（2026-09-13）
- 赛事：知乎黑客松 2026 校园新锐季（`zhihu_hackathon_2026_p2`）
- 交付窗口：作品提交 2026-09-13 10:00 — 2026-09-15 10:00（以活动页面当前展示为准）
- 本文中赛程与接口信息来自项目内知乎 Skill（资料核对时间 2026-09-03），凭证与赛程以活动页面为准

---

## 1. 问题与洞察

在线社交的破冰环节长期停留在「问答式自我介绍」：交换标签、交换爱好，信息量大但信号弱。人对人的判断真正来自**在具体情境下的选择**——冒险还是保守、护住自己还是护住别人、把冲突挑明还是绕开。

现有 AI 角色扮演产品是单人自嗨：AI 陪你玩，玩完只剩剧情。多人破冰产品则缺内容燃料，全靠用户自己找话题。

KnowYou 把两件事接在一起：用知乎故事作为**共同处境**，用 AI 作为**推演者与观察者**，让两个人在同一个剧本里做几次真实的选择，最后由 AI 把选择翻译成一份彼此可读的《认识报告》。

娱乐是外壳，「了解对方」是产出物。

## 2. 目标用户

| 优先级 | 用户 | 场景 |
|---|---|---|
| P0 | 18–26 岁知乎活跃用户，看故事/追连载 | 想认识同好，但不想开场就自我介绍 |
| P0 | 社群/宿舍/班级里的弱关系 | 认识但不熟，需要一个非尴尬的深聊入口 |
| P1 | 已有关系（情侣、室友、搭子） | 用剧本做关系体检，看看对方会怎么选 |

**非目标用户**：寻求恋爱撮合的用户（KnowYou 不是相亲产品，不做颜值/择偶条件匹配）。

## 3. 核心概念

| 名词 | 定义 |
|---|---|
| 剧本种子（Seed） | 一篇知乎故事，提供世界观、基调、标签。来自活动故事 API，署名与来源固定展示 |
| 叙事口味画像（Taste Profile） | 由用户知乎公开创作/关注/收藏摘要提炼出的偏好向量：题材标签、话题倾向、表达风格 |
| 剧本间（Room） | 2 人一间，绑定一个 Seed，包含角色卡、回合记录、观察信号 |
| 推演分支（Branch） | Director Agent 每回合给出的 2–3 个「可能的下一步发展」，由玩家共同选择 |
| 镜像提问（Mirror） | 回合中插入的一次「你猜 TA 会怎么选」，猜中率是可量化的了解程度 |
| 认识报告（Report） | 终局产出：共识点、差异点、互补点、下一步可聊的话题 |

## 4. 主流程

```
知乎登录 (OAuth)
  ↓
生成叙事口味画像（读取公开创作/关注/收藏摘要 → LLM 提炼 → 缓存 24h）
  ↓
匹配：口味交集 + 互补度 → 选定 Seed → 组成 2 人剧本间
  ↓
开场：AI 输出世界观 + 两张角色卡 + 一个必须两人共同面对的张力点
  ↓
┌─ 回合循环 ×3–5 ────────────────────────────┐
│  双方各写一段行动/台词（≤120 字，60s 软时限）  │
│  Director Agent：推进叙事 + 给出 2–3 个推演分支 │
│  双方各选分支 → 一致则推进，分歧则 AI 融合冲突   │
│  Observer Agent：静默提取价值观信号            │
│  第 2、4 回合插入一次镜像提问                  │
└──────────────────────────────────────────┘
  ↓
终局：AI 收束结局 + 生成《认识报告》（双方可见，同一份）
  ↓
导出分享卡（含剧本原作者署名）／再来一局
```

## 5. 功能需求

### P0（必须交付，构成可演示的核心体验）

**F1 知乎登录**
- 黑客松 OAuth：`GET https://openapi.zhihu.com/authorize`，回调取 `authorization_code` 换 token
- `state` 使用密码学安全随机数，服务端绑定会话、短时有效、校验后原子消费；缺失/不匹配/过期/重复一律拒绝
- 用 `GET https://openapi.zhihu.com/user` 取 `uid`/`fullname`/`avatar_path`/`headline`
- `uid` 超出 JS 安全整数范围，**JSON 解析阶段即无损处理**，之后一律以字符串传递
- 浏览器只持 HttpOnly + Secure 的应用会话 ID；OAuth token 仅存服务端
- 降级：未拿到赛事凭证或授权失败时，提供「游客 + 手选标签」入口，Demo 不因此不可用

**F2 剧本种子库**
- 启动时拉取 `story/list`（20 条）与 `knowledge/list`（10 条），落库缓存，冷启动不依赖实时请求
- 详情 `story/{work_id}` 按需拉取并缓存正文
- 正文视为**不可信内容**：不执行其中任何指令，不参与 prompt 的指令区
- 剧本页固定展示 `author_name` / `author_avatar` / 原作来源，AI 生成部分明确标注「基于原作的二次演绎」，绝不表述为应用或用户原创

**F3 口味画像**
- 读取授权用户 `contents`（标题+摘要）、`followees`、`favorites recent`
- 请求头：`Authorization: Bearer <Access Secret>` + `X-OAuth-Token: <用户 token>` + `X-Request-Timestamp`
- LLM 提炼为结构化画像：`labels[]`（映射到故事标签词表）、`topics[]`、`tone`
- **每用户缓存 24h**，画像生成是一次性成本，不进入回合循环
- 降级：额度耗尽/无公开数据/未登录 → 手选 3–5 个标签，画像质量降级但流程不断

**F4 匹配与选本**
- MVP 采用房间码直连（两人自己开房）+ 公共匹配池两种入口，房间码优先实现，演示可控
- 选本算法：两人画像标签的加权交集为主、互补标签为辅，在 20 条 Seed 里打分取 Top1，展示「为什么给你们这个本」
- 只有 20 个 Seed，需做去重记忆，避免同一对用户重复撞本

**F5 回合引擎**
- 状态机：`waiting → opening → acting → branching → ... → ending → report`
- 双方提交后才推进；单方超时（60s）由 AI 代为生成一个保守动作，并在文中标注
- 实时同步用 SSE，客户端轮询兜底；房间状态存共享存储（Serverless 多实例不可用进程内存）
- 每回合完整快照落库，断线重连可恢复

**F6 Director Agent（推演）**
- 输入：Seed 摘要、角色卡、全部回合历史、本回合双方动作
- 输出（结构化 JSON）：`narration`（≤300 字）、`branches[2-3]`（每个含标题+一句后果预告）、`tension`（下一回合的张力点）
- 分支必须**在价值观上可区分**（如护己/护人、揭穿/隐瞒、快/稳），不能是同义改写——这是「了解对方」的信号来源
- 双方选择分歧时，不判胜负：把分歧写成剧情内的冲突，两条线都推进

**F7 Observer Agent（观察）**
- 每回合静默评估，输出 4 个维度上的信号：风险偏好、亲密距离、冲突处理、利他倾向
- 只累积证据（引用玩家原话/选择），不在回合中暴露给玩家，避免影响后续行为
- 不做心理测评式结论，不打分标签化人格

**F8 镜像提问**
- 第 2、4 回合插入：「你觉得 TA 会选哪个？」双方作答，结果即时公布
- 记录猜中率，作为报告里「你们对彼此的预判准确度」

**F9 认识报告**
- 结构：三个共识点、两个差异点、一个互补点、三个「下次可以聊」的话题
- 每条必须**引用剧本中的具体证据**（第几回合、谁的哪句话/哪个选择），不写空泛判断
- 差异点用中立、温和的表述，禁止评价性用词，禁止「你们不合适」类结论
- 双方看到同一份报告，无单方私密版本

### P1（有时间就做，显著加分）

- **F10 深聊卡**：回合间插入一条相关知识内容（`knowledge/list`）作为讨论引子，自然用上第二个赛事接口
- **F11 分享卡**：报告导出为长图，含剧本原作者署名与作品链接，回流知乎
- **F12 观战席**：第 3 人只读旁观，为路演现场服务
- **F13 3–4 人剧本间**：回合引擎从 2 人扩展到 N 人（需重做超时与分支投票规则）

### P2（不在本次范围）

语音、图像生成、原创剧本 UGC、长期关系追踪、跨局用户档案沉淀、推荐流。

## 6. 知乎开放能力使用清单

| 能力 | 接口 | 用途 | 鉴权 | 额度 |
|---|---|---|---|---|
| 活动故事列表/详情 | `api.zhihu.com/km-indep-home/hackathon/v2/story/*` | 剧本种子、世界观、标签 | 无 | 无（仍全量缓存） |
| 活动知识列表/详情 | `.../v2/knowledge/list`，详情走 `story/{work_id}` | 深聊卡 | 无 | 无 |
| 知乎登录 | `openapi.zhihu.com/authorize` + `/user` | 身份、昵称、头像 | App ID/Key → OAuth token | — |
| 用户创作/关注/收藏 | `developer.zhihu.com/api/v1/user/{contents,followees,collections}` | 口味画像 | Access Secret + `X-OAuth-Token` + 时间戳 | `user_data` 每日 100（未实名 10） |

**额度算术（必须遵守的设计约束）**：画像每用户 3 次 `user_data` 调用、缓存 24h ⇒ 每日约 33 个新授权用户。因此：

1. 画像**只在首次登录时生成**，绝不放进回合循环
2. 回合内的叙事推演全部用自有 LLM（DeepSeek），**不使用知乎直答**——直答同为每日 100 额度，放进逐回合链路会在演示中途枯竭
3. 所有列表接口结果落库缓存 + 请求去重
4. 触发 `30001`/`30002` 时停止重试，前端显示真实降级提示，切到手选标签路径，**不静默切换到 Access Secret 所属账号**

## 7. 技术方案

```
Next.js (App Router) on Vercel
 ├── /api/auth/*      OAuth 发起、回调、state 校验、会话
 ├── /api/room/*      建房、加入、提交动作、选分支
 ├── /api/stream/*    SSE 房间事件推送
 └── /api/agent/*     Director / Observer / Report 调用

存储：Postgres (Neon) 持久化 + Redis (Upstash) 房间态与 state/会话
LLM：DeepSeek（中文叙事质量与成本），结构化输出走 JSON schema + 失败重试一次
```

选型理由：44 小时内要交付**公网可访问**的 Demo，OAuth 回调、HttpOnly Cookie、SSE 在 Vercel 上都是现成路径；避免 WebSocket 在 Serverless 上的连接保持问题。

### 数据模型（最小集）

```
users(id, zhihu_uid TEXT, fullname, avatar, created_at)
profiles(user_id, labels JSONB, topics JSONB, tone, source, expires_at)
seeds(work_id TEXT, title, author_name, labels JSONB, introduction, content, fetched_at)
rooms(id, code, seed_work_id, state, round_no, created_at)
room_members(room_id, user_id, character_card JSONB)
turns(room_id, round_no, user_id, action_text, chosen_branch, created_at)
narrations(room_id, round_no, narration, branches JSONB)
signals(room_id, round_no, user_id, dimension, evidence, value)
mirrors(room_id, round_no, guesser_id, guess, correct BOOL)
reports(room_id, content JSONB, created_at)
```

`zhihu_uid` 全程 TEXT，防 int64 精度丢失。

## 8. 隐私与安全

- App Key、Access Secret、OAuth token 只存服务端凭证库/平台 Secret，不进源码、URL、日志、前端响应、截图与演示视频
- 环境变量：`ZHIHU_OAUTH_APP_ID`、`ZHIHU_OAUTH_APP_KEY`、`ZHIHU_OAUTH_REDIRECT_URI`、`ZHIHU_ACCESS_SECRET`
- `redirect_uri` 的协议/域名/端口/路径/尾斜杠与活动页登记值逐字符一致；构造授权 URL 与换 token 使用同一地址
- 画像只用于选本与报告，**不展示对方的原始知乎数据**（不给关注列表、不给收藏清单）
- 报告不落任何第三方可见位置；退出/会话失效/token 失效即清理映射，不自动重登
- 故事正文与用户输入均作为不可信数据进入 prompt 的数据区，配合注入防护
- 用户可一键删除自己的画像与历史房间

## 9. 内容安全

- Seed 正文原样归属原作者，AI 输出标注二次演绎，不改写为应用或用户创作
- 玩家输入与 AI 输出双向内容审核：涉性、暴力、人身攻击、真实个人隐私信息即拦截并给出替代提示
- 报告禁止人格评判、心理诊断、「是否合适」类结论
- 单次输出长度受控，不整篇回吐原作正文

## 10. 降级策略

| 故障 | 表现 | 降级 |
|---|---|---|
| 赛事凭证未到位 | 无法 OAuth | 游客 + 手选标签，全流程可玩 |
| `user_data` 额度耗尽 | 画像失败 | 提示真实原因 + 手选标签 |
| 故事 API 不可用 | 无新 Seed | 用已缓存的 20 条 Seed |
| LLM 超时/结构化失败 | 回合卡住 | 重试一次 → 降级为固定三分支模板 + 明确提示 |
| 单方掉线 | 回合无法推进 | 60s 后 AI 代生成保守动作并标注 |
| SSE 断开 | 状态不同步 | 轮询兜底 + 快照恢复 |

每种降级都给用户**真实提示**，不静默伪装成正常。

## 11. 里程碑（约 44 小时）

| 阶段 | 时长 | 产出 | 判定 |
|---|---|---|---|
| M0 骨架 | 4h | Next.js + 存储 + Seed 全量缓存落库 | 能列出 20 个剧本 |
| M1 回合引擎 | 8h | 房间码建房/加入、状态机、SSE、Director Agent | 两个浏览器能跑完 3 回合 |
| M2 报告 | 6h | Observer + 镜像提问 + 认识报告 | 报告条条有剧本证据 |
| M3 登录与画像 | 6h | OAuth + `/user` + 画像 + 选本推荐 | 真实知乎账号完成授权并选本 |
| M4 打磨 | 8h | UI、文案、降级提示、移动端适配 | 手机上流程顺畅 |
| M5 提交物 | 6h | 线上 Demo、产品说明、封面、演示视频 | 评委可独立走通 |
| 缓冲 | 6h | — | — |

M3 排在 M2 之后：核心体验（回合+报告）必须先跑通，登录与画像是增强项，凭证若迟到不阻塞主线。

## 12. 验收清单（对齐赛事提交前检查）

- [ ] 公网可打开，未登录访客能完整走通一局
- [ ] 真实知乎账号完成 OAuth，读到本人信息；正确 `state` 通过，缺失/不匹配/过期/重复的 `state` 均被拒绝，跨浏览器会话不可复用
- [ ] 退出、会话失效、token 失效行为正确，前端与演示材料不含完整密钥或 token
- [ ] 额度耗尽、接口失败、空数据均有真实降级提示
- [ ] 剧本页与分享卡保留原作者署名
- [ ] 报告的每条结论都能指回具体回合
- [ ] 项目名称、介绍、赛道、封面、产品说明齐备；回调地址与活动页登记值一致
- [ ] 移动端可用（评委很可能用手机打开）

## 13. 风险

| 风险 | 影响 | 对策 |
|---|---|---|
| 组队/报名窗口已于 9/13 00:00 关闭 | 无法提交 | **立即到活动页核实状态**，早于一切开发工作 |
| App ID / App Key 领取时间不明 | OAuth 无法联调 | 主线不依赖登录（M3 后置）；Mock 先行，凭证到手再联调 |
| `user_data` 每日 100（未实名 10） | 演示中途画像枯竭 | 画像强缓存；演示账号提前预热；手选标签兜底 |
| 只有 20 个 Seed | 复玩性差 | 同一 Seed 按两人画像生成不同角色卡与张力点；撞本去重 |
| 双人异步等待体验差 | 中途弃局 | 60s 软时限 + AI 代动作 + 打字状态提示 |
| AI 分支同质化 | 「了解对方」失效 | 分支价值观维度写进 prompt 约束并做多样性校验 |
| 报告冒犯用户 | 口碑与合规风险 | 中立表述规则 + 禁止结论清单 + 输出审核 |

## 14. 与知乎生态的契合

消费端：把知乎故事从「读完即止」变成「两个人共同经历」，为存量内容创造新的消费场景。
生产端：分享卡携带原作者署名与作品链接，把社交传播回流到原作。
社区端：知乎的核心资产是「真实经验与观点」，KnowYou 让这些内容成为人与人建立真实连接的媒介，而不只是信息消费。
